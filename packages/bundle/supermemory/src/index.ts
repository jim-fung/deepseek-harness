/**
 * @deepseek-ai/dsh-supermemory — opt-in hosted memory over supermemory.ai as a
 * profile bundle patch layer. The package's substance is `cordis.patch.yml`,
 * declared by the `dsh.bundle.patch` manifest field and resolved by the profile
 * composer through that field; this module carries no runtime API.
 * @module @deepseek-ai/dsh-supermemory
 */

export {}
